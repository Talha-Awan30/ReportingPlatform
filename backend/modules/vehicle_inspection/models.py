"""Extra tables for the Vehicle Inspection module.

Most modules need none - checkpoint answers live in `Report.data`. Define models
here only when this category needs relational data of its own, then run
`flask --app app db migrate`.
"""
# from extensions import db
# from models.base import BaseModel
