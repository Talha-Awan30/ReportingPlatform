"""Extra tables for the Slings & Lifting Gear module.

Most modules need none - checkpoint answers live in `Report.data`. Define models
here only when this inspection item needs relational data of its own, then run
`flask db migrate`.
"""
# from extensions import db
# from models.base import BaseModel
